Strukturen für eingabe:
    Liste
        pro: schöen formatierung
             flexible (alte defintionen mehrfach verwenden)
        con: keine abhängigkeiten direkt in erstellung erkennbar
    workspace(tabs(dropdown(panell(commandcontorl(definiton)))))
        pro: direkte erstellung implementierbar
             hierachie ersichtlich
        con: auch keine abhängigkeiten direkt erkannbar
             unüberichtliche defintion
    workspace().tab().toolbar().dropdown().apnel().commandcontrol(definition)
        pro: abhängigkeit direkt bei definition
        con: unübersichtliche formatierung
                   
--> Verwedung von Liste 
--> wo wird reihenfolde validiert?